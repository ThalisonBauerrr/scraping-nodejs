<!-- views/login.ejs -->
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>LethalCode</title>
    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="/css/botoes.css">
    <link rel="stylesheet" href="/css/startPage.css">
    <link rel="stylesheet" href="/font/font.css">
     <!-- Caminho para o seu arquivo de estilo -->
</head>

<body>
    
     <!-- aqui enseri header -->
     <%- include('partials/header') %>
     <div class="principal">
        
        <div id="leftbar"></div>
        <div id="page-container">
            
                    <% if (user.conta_blaze) { %>
                        <h3>Conta Blaze</h3>
                        <p>📌 Sua conta Blaze está vinculada como: <strong><%= user.conta_blaze %></strong></p>
                        <p style="color: red;">❌ O cadastro é único e não pode ser alterado.</p>
                    <% } else { %>
                        <h3>Vincular Conta Blaze</h3>
                        <form action="/set-blaze-account" method="POST">
                            <label for="emailBlaze">E-mail da Blaze:</label>
                            <input type="email" id="emailBlaze" name="emailBlaze" required>
                            <label for="emailBlaze">Senha da Blaze:</label>
                            <input type="password" id="password_blaze" name="password_blaze" required>
                            <label for="emailBlaze">Confirma senha da Blaze:</label>
                            <input type="password" id="confirma_password_blaze" name="confirma_password_blaze" required>
                            <button type="submit">Vincular Conta</button>
                        </form>
                    <% } %>

                    <% if (typeof success !== 'undefined' && success) { %>
                        <p style="color: green;"><%= success %></p>
                    <% } %>

                    <% if (typeof error !== 'undefined' && error) { %>
                        <p style="color: red;"><%= error %></p>
                    <% } %>
        </div>
    </div>
 <!-- Incluindo o JavaScript -->
 <script src="/js/startPage.js"></script> <!-- Script global -->
</body>
</html>